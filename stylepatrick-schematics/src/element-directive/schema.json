{
  "$schema": "http://json-schema.org/schema",
  "$id": "ElementDirectiveSchema",
  "title": "My Element Directive Schema",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the directive.",
      "type": "string"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the directive.",
      "visible": false
    }
  },
  "required": [
    "name"
  ]
}
